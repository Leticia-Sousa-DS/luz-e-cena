import MenuItem from '../../Menu/MenuItem';
import MenuList from '../../Menu/MenuList';


const HeaderLinks = () => {
    return (
        <MenuList>
            <MenuItem>Eventos</MenuItem>
            <MenuItem>Clube de Fidelidade</MenuItem>
            <MenuItem>Sobre Nós</MenuItem>
        </MenuList>
    )
};

export default HeaderLinks;